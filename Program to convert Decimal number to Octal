//Program to convert Decimal number to Octal
#include<stdio.h>
int main(){
    int n, bin=0;
    printf("Enter the Number: ");
    scanf("%i", &n);
    int temp = n;
    int place = 1;

    while (temp != 0){
        int rem = temp%8;
        bin = bin + rem * place; 
        temp /= 8;
        place *= 10;
    }
    printf("Binary of %i is %i", n, bin);
    return 0;
}
