#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

#include <stdio.h>

int main() {
    int n;
    scanf("%d", &n); // Read number of elements
    int arr[n];

    for (int i = 0; i < n; i++) {
        scanf("%d", &arr[i]); // Read array elements
    }

    int result[n]; // Array to store unique elements
    int size = 0;  // Counter for unique elements

    for (int i = 0; i < n; i++) {
        int alreadyExists = 0;

        // Check if element already in result[]
        for (int j = 0; j < size; j++) {
            if (arr[i] == result[j]) {
                alreadyExists = 1;
                break;
            }
        }

        // If not found, add to result[]
        if (!alreadyExists) {
            result[size] = arr[i];
            size++;
        }
    }

    // If all were duplicates
    if (size == 0)
        printf("No elements left");
    else {
        for (int i = 0; i < size; i++) {
            printf("%d ", result[i]);
        }
    }

    return 0;
}
