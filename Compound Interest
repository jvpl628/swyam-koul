#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() { 
    int P, T; 
    float R, CI; 
    scanf("%d", &P); 
    scanf("%f", &R); 
    scanf("%d", &T); 
    CI = P * (pow((1 + R/100), T) - 1); 
    printf("%.2f", CI);
   
    return 0;
}
