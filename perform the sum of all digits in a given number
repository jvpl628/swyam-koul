//wap to perform the sum of all the digits in a given number
#include<stdio.h>
#include<limits.h>
int main(){
    int num,sum=0;
    printf("enter the number");
    scanf("%D"<&num);
    while (num!=0){
        sum=sum+num%10;
        num=num/10;
    }
    printf("%D\n",sum);
    return 0;
}
