#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
     char ch; 
    int num; 
    float f; 
    double d;
    scanf("%c %d %f %lf", &ch, &num, &f, &d); 
    printf("%c %d %.2f %.6lf \n", ch, num, f, d);
    return 0;
}
