#include <stdio.h>int main() 
#include <string.h>
#include <math.h>
#include <stdlib.h>int main() 
    int main() {
    int N, sum = 0;
    scanf("%d", &N);

    for (int i = 1; i <= N; i += 2) {
        sum += i;
    }

    printf("%d\n", sum);

    return 0;
}
