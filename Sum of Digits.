#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>
int main() {
    int num, sum=0;
    scanf("%d",&num);
    while(num != 0){
        int lastDigit = num%10;
    sum = sum+ lastDigit;
    num = num/10;
    }
    printf("%d", sum);
    return 0;
}
